SELF_01
Что такое бэкенд в веб-приложении?
Визуальная часть приложения, с которой взаимодействует пользователь
Серверная часть, которая обрабатывает запросы, выполняет бизнес-логику и работает с данными (answer)
Система управления стилями и макетами
Инструмент для отрисовки интерфейса в браузере

Что является основным отличием микросервисной архитектуры от сервисно-ориентированной (SOA)?
Микросервисы максимально маленькие и выполняют только одну функцию (answer)
Микросервисы всегда объединяют несколько функций в один сервис
В SOA сервисы не могут взаимодействовать через API
Микросервисы не могут масштабироваться независимо

Какая технология позволяет использовать JavaScript на сервере?
Node.js (answer)
Vue.js
React
Angular

Что представляет собой клиент в клиент-серверной архитектуре?
Веб-браузер или мобильное приложение, которое отправляет запросы и отображает ответы сервера (answer)
API, через которое общаются разные сервисы
База данных
Физический компьютер или облачный сервер, выполняющий логику приложения

Что происходит, когда пользователь вводит URL в браузере?
Клиент сохраняет страницу в локальном хранилище без обращения к серверу
Клиент отправляет HTTP-запрос, сервер его обрабатывает и возвращает ответ (answer)
Браузер напрямую подключается к базе данных
Сервер выполняет только генерацию интерфейса без логики

Какое преимущество даёт разделение фронтенда и бэкенда в headless-архитектуре?
Сокращение числа API-запросов до нуля
Гарантированное использование только одного языка программирования
Возможность независимо изменять интерфейс и серверную логику (answer)
Упрощённая работа с единым HTML-шаблоном

Какое преимущество характерно для микросервисной архитектуры?
Отсутствие сетевых задержек
Единая кодовая база для всех компонентов
Упрощённая отладка по сравнению с монолитом
Независимое масштабирование отдельных сервисов (answer)

Что означает термин headless-архитектура?
Использование исключительно микросервисов
Монолитное объединение клиентской и серверной частей
Архитектура, где бэкенд сам генерирует интерфейс
Разделение фронтенда и бэкенда, взаимодействующих через API (answer)

Какая роль у базы данных в архитектуре бэкенда?
Хранение и предоставление информации по запросу приложения (answer)
Отображение страниц на стороне клиента
Генерация пользовательского интерфейса
Управление CSS и HTML

Какой пример взаимодействия фронтенда и бэкенда через REST API является корректным?
Фронтенд запрашивает у бэкенда список пользователей, а бэкенд возвращает JSON-данные (answer)
Фронтенд напрямую обращается к базе данных без участия бэкенда
Фронтенд передает на сервер только CSS-стили
Бэкенд передает фронтенду только HTML-страницы

Что означает принцип stateless в REST API?
Сервер не хранит состояние клиента между запросами (answer)
Клиент обязан всегда передавать токен в cookies
Сервер хранит сессии пользователей в памяти
Данные передаются только в формате XML

Какая особенность характерна для монолитной архитектуры при работе с кодовой базой?
Вся логика приложения и компоненты находятся в одном проекте (answer)
Каждый модуль разворачивается отдельно и независимо
Данные всегда распределены по множеству баз данных
Для разных частей обязательно используются разные языки программирования

Какой формат данных чаще всего используется для обмена информацией между фронтендом и бэкендом через REST API?
XLSX
JSON (answer)
XML
CSV

Какая из задач входит в обязанности бэкенд-разработчика?
Разработка API для связи бэкенда с фронтендом (answer)
Верстка HTML-шаблонов
Создание макетов интерфейса
Написание CSS-стилей для страниц

Что является основным недостатком микросервисной архитектуры?
Ограничение на использование разных технологий
Единая база данных для всех модулей
Отсутствие гибкости и масштабируемости
Сложность мониторинга и отладки при большом количестве сервисов (answer)

Что из перечисленного является примером бизнес-логики в бэкенде?
Создание шрифтов и иконок
Отображение кнопки на странице
Проверка корректности введённых пользователем данных (answer)
Стилизация формы с помощью CSS





SELF_02
Что делает функция require() в Node.js?
Загружает и подключает модуль в программу (answer)
Компилирует JavaScript в машинный код
Инициализирует проект Node.js
Создаёт новый поток

Для чего используется директория node_modules в проекте?
Для хранения конфигурационных файлов
Для хранения установленных пакетов и их зависимостей (answer)
Для запуска сервера
Для хранения исходного кода проекта

Какая из характеристик относится к Node.js?
Кроссплатформенность и открытый исходный код (answer)
Только для Windows
Только для macOS
Используется исключительно для браузеров

Что такое Node.js?
Операционная система
Язык программирования
Среда выполнения JavaScript, позволяющая запускать код вне браузера (answer)
Фреймворк для разработки веб-приложений

Какая команда создаёт новый файл package.json?
npm update
npm install
npm init (answer)
npm start

Какой объект в Node.js используется для доступа к переменным окружения?
system.env
process.env (answer)
env.config
env

Что означает неблокирующий ввод-вывод (non-blocking I/O) в Node.js?
Каждая операция выполняется строго последовательно
Для каждой операции создаётся новый процесс
Node.js не поддерживает работу с файлами
Операции ввода-вывода выполняются асинхронно, не блокируя основной поток выполнения программы (answer)

Что означает первая цифра в записи версии пакета 1.2.3 по правилам семантического версионирования?
Мажорная версия (несовместимые изменения) (answer)
Номер сборки
Минорная версия (новые функции)
Патч (исправления ошибок)

Каким образом в Node.js обрабатываются завершения асинхронных операций?
Автоматически, без участия разработчика
С помощью функции sleep()
Только через циклы while
Через конструкции Promise/async-await (answer)

Что такое npm?
Отдельный язык программирования
Фреймворк для веб-разработки
Система контроля версий
Встроенный менеджер пакетов для Node.js (answer)

Что такое event loop в Node.js?
Отдельный поток для выполнения асинхронных операций
Библиотека для работы с файлами
Метод из пакета npm
Механизм, отслеживающий события и задачи, которые должны быть выполнены (answer)

Для чего чаще всего используют переменные окружения в Node.js?
Для хранения HTML-шаблонов
Для хранения конфиденциальной информации и настройки окружений (dev, test, prod) (answer)
Для запуска браузера Chrome
Для компиляции JavaScript в C++

Какой командой запускается JavaScript-файл в Node.js?
run имя_файла.js
js имя_файла.js
node имя_файла.js (answer)
execute имя_файла.js

На каком движке работает Node.js?
Chakra
SpiderMonkey
Chrome V8 (answer)
NodeEngine

Какой символ в package.json позволяет обновлять только патчи внутри одной минорной версии?
~ (answer)
*
!
^

Какая команда создаёт package.json сразу с настройками по умолчанию, без дополнительных вопросов?
npm new
npm config
npm init -y (answer)
npm install

Какой встроенный модуль Node.js используется для работы с файловой системой?
path
http
os
fs (answer)

Какое назначение поля "main" в package.json?
Указывает автора проекта
Указывает список зависимостей проекта
Определяет точку входа в приложение (основной файл) (answer)
Хранит переменные окружения




SELF_03
Какую основную задачу решают фреймворки при создании веб-приложений?
Заменяют веб-сервер
Автоматически создают дизайн интерфейса
Уменьшают потребление оперативной памяти
Предоставляют готовые решения для типовых задач и ускоряют разработку (answer)

Что представляет собой фреймворк в контексте веб-разработки?
Набор библиотек, инструментов и стандартов, упрощающих создание приложений (answer)
Система управления базами данных
Графический интерфейс для тестирования приложений
Среда выполнения JavaScript-кода

Какие особенности делают Express удобным для разработки серверных приложений?
Простая настройка маршрутов и обработки запросов (answer)
Невозможность менять архитектуру проекта
Возможность использования всех функций Node.js (answer)
Наличие встроенной базы данных
Подключаемость дополнительных модулей при необходимости (answer)
Работа только с шаблонами HTML

Какой первый шаг необходимо выполнить перед установкой Express в новом проекте Node.js?
Настроить переменные окружения
Установить глобально пакет http
Скопировать исходный код Express из GitHub
Инициализировать проект с помощью команды npm init -y (answer)

Какая команда используется для установки Express в проект?
node install express
npm install express (answer)
npm create express
express new app

Для чего используется пакет nodemon при разработке Express-приложений?
Для тестирования производительности сервера
Для компиляции JavaScript в машинный код
Для автоматического перезапуска сервера при изменении исходных файлов (answer)
Для кэширования HTTP-запросов

Что делает View в архитектуре MVC?
Реализует маршрутизацию запросов
Проверяет корректность данных
Сохраняет изменения в базе данных
Отображает данные пользователю и формирует визуальный интерфейс (answer)

Что представляет собой Express.JS?
Средство для тестирования клиентских скриптов
Графический интерфейс для Node.js
Минималистичный и гибкий фреймворк для Node.js, упрощающий создание серверных приложений (answer)
Отдельный язык программирования на базе JavaScript

Какую задачу выполняет модель при взаимодействии с контроллером?
Управляет состоянием клиентского приложения
Перенаправляет пользователя на другую страницу
Отправляет HTML-страницу контроллеру
Выполняет операции с данными (чтение, запись, проверка корректности) (answer)

Какую роль выполняет Controller в MVC?
Хранит шаблоны HTML-страниц
Сохраняет пользовательские данные в кэше
Принимает действия пользователя, взаимодействует с моделью и выбирает представление (answer)
Выполняет SQL-запросы напрямую

Какая функция фреймворка связана с повышением безопасности приложения?
Предоставление проверенных методов защиты данных и предотвращения уязвимостей (answer)
Автоматическое шифрование всех HTTP-заголовков
Ограничение доступа к Node.js-модулям
Удаление пользовательских данных из формы

Какая команда используется для запуска Express-сервера после создания файла app.js?
npm start app.js
npm start express
node app.js (answer)
run express app.js

Почему Express называют “не навязывающим архитектуру”?
Так как он работает только с REST API
Потому что Express не поддерживает MVC
Поскольку маршруты формируются автоматически
Потому что разработчик сам определяет структуру каталогов, файлов и подключаемых библиотек (answer)

Какую часть MVC-архитектуры чаще всего реализуют с помощью шаблонизаторов (EJS, Pug и др.
Controller
Model
View (answer)
Middleware

Что является основной целью использования архитектурного шаблона MVC при разработке приложений?
Упрощение поддержки и структурирования кода (answer)
Обеспечение графического интерфейса
Ускорение выполнения программы
Снижение использования оперативной памяти





SELF_04
Что произойдёт, если пользователь обратится к маршруту, который не определён в приложении Express?
Express перезапустит сервер
Приложение автоматически создаст новый маршрут
Приложение вернёт 403 Forbidden
Приложение вернёт ошибку 404 Not Found (answer)

Какой HTTP-метод обычно используется для запроса данных с сервера, не изменяя их?
DELETE
GET (answer)
PUT
POST

Какой из параметров функции middleware отвечает за передачу управления следующему обработчику?
res()
route()
next() (answer)
req()

Для чего используется express.Router() в приложении Express?
Для подключения middleware только к POST-запросам
Для автоматической генерации API-документации
Для создания независимых групп маршрутов, которые можно подключать к основному приложению (answer)
Для обработки ошибок при маршрутизации


Соотнесите элементы структуры Express-приложения с их назначением.

routes/
Определение маршрутов и связь с контроллерами (answer)
middleware/
Промежуточные функции обработки запросов (answer)
controllers/
Логика обработки запросов (answer)


Какова основная роль контроллера в архитектуре MVC?
Отвечать за визуальное отображение контента пользователю
Хранить данные и управлять базой данных
Определять маршруты и подключать middleware
Обрабатывать запросы и управлять взаимодействием между моделью и представлением (answer)


Как подключить middleware, которое должно выполняться для всех маршрутов в приложении Express?
app.apply(<middleware>)

app.use(<middleware>) (answer)

app.global(<middleware>)

app.route(<middleware>)


Если ни один из маршрутов не совпадает с запросом, Express возвращает ответ с кодом  404 (answer) , что означает «Ресурс не найден (answer)».


Какое поведение будет у следующего кода?
app.get("/special", logRequests, (req, res) => {
  res.send("This is a special route");
});
Middleware logRequests выполнится для всех маршрутов
Middleware logRequests не выполнится
Middleware logRequests вызовет ошибку
Middleware logRequests выполнится только для маршрута /special (answer)


Что произойдёт, если внутри middleware не вызвать функцию next() и не отправить ответ?
Возникнет ошибка 404
Express автоматически завершит запрос
Express перезапустит middleware
Запрос "зависнет" и не будет обработан (answer)


Как получить значение query-параметра term из запроса к /search?term=nodejs&page=2?
req.params.term
req.headers.term
req.body.term
req.query.term (answer)

Что такое маршрутизация в веб-приложении?
Определение путей, по которым пользователь может перемещаться внутри приложения
Метод кэширования данных на клиенте
Механизм шифрования URL-адресов
Процесс распределения нагрузки между серверами

Какой из приведённых примеров показывает правильное подключение контроллера к маршруту?
app.route("/books", listBooks);
app.get("/books", listBooks());
app.use("/books", listBooks());
app.get("/books", listBooks); (answer)

Что такое middleware в контексте Express.js?
Это отдельный сервер, обрабатывающий все запросы до Express
Это функция, которая выполняется между получением запроса и отправкой ответа (answer)
Это библиотека для подключения баз данных
Это файл конфигурации маршрутов

Почему в Express рекомендуется выносить логику маршрутов в контроллеры?
Чтобы Express мог автоматически генерировать документацию
Чтобы маршруты работали быстрее
Чтобы код был более читаемым, организованным и удобным для тестирования (answer)
Чтобы уменьшить нагрузку на сервер

Что обозначает двоеточие (:) в определении маршрута Express, например /items/:id?
Обозначает query-параметр
Указывает, что маршрут будет доступен только для авторизованных пользователей
Указывает на необходимость middleware перед маршрутом
Обозначает параметр маршрута, значение которого извлекается из URL (answer)

Соотнесите свойства объекта req (объекта запроса в Express) с их правильными описаниями.

req.query
Содержит query-параметры из URL после знака ? (answer)
req.headers
Содержит список всех заголовков запроса (answer)
req.params
Содержит значения параметров маршрута, указанных через :id (answer)
req.body
Содержит тело запроса, например данные формы (answer)

Что делает следующий фрагмент кода?
app.use('/books', bookRouter);
Подключает роутер bookRouter, все маршруты которого начинаются с /books (answer)
Подключает контроллер bookController напрямую
Устанавливает глобальное middleware для всех маршрутов
Запускает сервер на порту 3000


